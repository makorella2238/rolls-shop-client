export const initialized = (state: any) => {
    return state.app.initialized
};

export const isAuth = (state: any) => {
    return state.app.isAuth
};

export const username = (state: any) => {
    return state.app.username
};

export const updatingCounts = (state: any) => {
    return state.app.updatingCounts
};
